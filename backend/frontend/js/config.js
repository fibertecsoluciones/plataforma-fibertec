// Si sirves el frontend DESDE el mismo backend de Express (como ya está configurado
// en server.js), deja esto vacío ('') y todo funcionará con rutas relativas /api/...
//
// Si el frontend lo subes aparte (otro servicio en Railway, Netlify, etc.), pon aquí
// la URL pública de tu backend, ej: 'https://mi-backend.up.railway.app'
window.API_BASE_URL = '';
